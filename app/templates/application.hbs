<div>
{{#paper-toolbar tall=true as |toolbar| }}
  <div class="layout-row flex">
    <div class="flex" style="position:relative;max-width:20%;max-height:100%;">
      <img src="/img/img_IPFMS.png" style="position: absolute;bottom:10%;width:400px;max-width:100%;max-height:100%;">
    </div>
    <div class="layout-column flex">
      <div class="flex">
        <span class="flex"></span>
      </div>
      <div class="flex">
        {{#toolbar.tools}}
          {{#paper-button raised=true href="/"}}
            {{paper-icon "home"}}
            Home
          {{/paper-button}}
          {{#paper-button raised=true href="/config"}}
            Configuration
          {{/paper-button}}
          {{#paper-button raised=true href="/print"}}
            Print
          {{/paper-button}}
          <span class="flex"></span>
          {{#paper-button mini=true href="/search"}}
            {{paper-icon "search"}}
          {{/paper-button}}
        {{/toolbar.tools}}
      </div>
    </div>
  </div>
{{/paper-toolbar}}
</div>
<div>
  {{outlet}}
</div>